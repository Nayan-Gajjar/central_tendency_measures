# generate 10 random numbers between 0 and 10
randomlist_even = runif(10, min=0, max=10)

# generate 9 random numbers between 0 and 10
randomlist_odd = runif(10, min=0, max=10)

# Median  ----------------------------------------------------------------------
even_median = median(randomlist_even)

odd_median = median(randomlist_odd)
